import 'package:jaspr/dom.dart';
import 'package:jaspr/jaspr.dart';

class ProjectsPage extends StatelessComponent {
  const ProjectsPage({super.key});

  @override
  Component build(BuildContext context) {
    return div([
      p([.text('En construcción 🚧')]),
    ]);
  }
}
